import numpy as np
import matplotlib.pyplot as plt
import load_data, backprop, gradient_descent

trainImg, trainLabel, testImg, testLabel = load_data.initialize()

### Implement neural network, forward propagation, cost function here. 
### This is the file to run.
